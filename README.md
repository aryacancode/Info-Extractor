# Log-analyzer

This project entails the creation of a Bash script to capture and present various system metrics for a Linux operating system. The script provides insights into the Linux network details, including private and public IP addresses and the default gateway. It extends to compute disk statistics, showcasing overall size, and free and used space. The script also highlights the five largest directories and monitors CPU usage.
